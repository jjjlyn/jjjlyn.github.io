# Stateful Hot Reload ??? (번개 모양 -> ^s)
상태를 유지한 채 ui가 즉각적으로 바뀜

# Dart
* _를 붙이면 private / 안 붙이면 public
* async의 return값은 future

lib directory (source code)
test directory -> pubspec.yaml (json과 비슷) (configure 파일)

#### Row 자기 기준
MainAxisAlignment

CrossAxisAlignment
* FAB (+) 
* FAB (-)

#### Column 자기 기준
MainAxisAlignment
CrossAxisAlignment

Static할 땐 Stateless를 쓴다. 성능 면에서는 이게 낫다.

개발 할 때는 거의 Stateful을 쓴다... Stateful widget은 state를 만들어서 연결시켜줘야 한다.
- State는 gui에 영향을 주는 data(멤버 변수)를 가지고 있다. State를 활용해서 data binding을 한다. 
- State 내부에는 build()도 같이 있고 이 안에서 widget tree를 구성한다.
- setState() : 화면 갱신

setState(
(anonymous parameter -- 빈 칸(익명함수일 때) 함수가 보통 들어간다){
});

'$_counter' <= String formatting (== _counter.toString())

* Widget
    * Stateful Widget
        * State (무조건 같이 create)
            * setState(갱신)
            * build() -> Widget Tree
            * state 내부에는 gui를 변화시키는 데이터 상태를 저장
            * hot reload가 됨
    * Stateless Widget
        * Static한 widget


* Safe Area

* Scaffold : 공사장 '단'의 느낌??
    * AppBar
    * FloatingActionButton 
    * PersistentFooterButtons
    * BottomNavigationBar

* 최상단에서 Safe Area와 Scaffold는 기본적으로 들어간다고 보면 된다.
* Row는 부모 Layout을 100% 다 먹는다. Column도 화면의 최대치를 먹는다.
* 부모의 크기를 얼마나 차지하는가?
    * Row는 부모의 크기를 모두 차지한다.
* 자식에게 얼마의 크기를 주는가? (본인의 크기)
    * 본인의 크기는 정해져 있지 않다

PageRoute -> Activity/Fragment의 개념

Navigator.push/pop 할 때는 습관적으로 async, await를 쓰는 것이 좋다.

DefaultTabController (위젯 형태로 만들어 버렸음)
* length : tab의 개수
* child : scaffold
    * appbar
        * child : tabbar
            이 안에는 각 tab들이 있음
    * body
        * children<widget>
            Text의 배열
            
Sizedbox는 이미지 크기 조절할 때 가장 가볍게 작동한다.
