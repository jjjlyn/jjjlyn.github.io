# Ubuntu에서 Jekyll 사용하기
1. 루비 설치
2. 지킬 설치(지킬은 동적으로 블로그의 변경 사항을 볼 수 있게 해줌)
* 관리자 모드 `sudo`를 접두에 붙일 것 (`superdo`)

# 검색
* 간단 검색 : ls
* 권한까지 포함 검색: ls -al

# 디렉토리 진입
* cd
* 전 디렉토리로 돌아가기: cd ..

# 폴더 지우기
* Remove folder(관리자 모드로 진입해서 삭제해야 할 경우에는 항상 `sudo`를 붙일 것) : (sudo) rm -rf <폴더명>
